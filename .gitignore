# environments
.venv/
__pycache__/
*.pyc

# secrets
.env

# the data tools fetch live and keyless every run; only the committed transcript below lives in data/



# jupyter
.ipynb_checkpoints/

# committed on purpose: assets/*.png (so charts render in the README without running),
# the executed notebooks/*.ipynb (so GitHub shows the output inline), and
# data/cached_agent_run.json (the illustrative cached Claude response, so the
# notebook teaches end-to-end with no API key).
